<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.leverages.careerticketcafe">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".views.NewsDetailActivity"></activity>
        <activity android:name=".views.NewsListActivity" />
        <activity android:name=".views.SeminarDetailActivity" />
        <activity android:name=".views.SeminarListActivity" />
        <activity android:name=".views.MenuDetailActivity" />
        <activity android:name=".views.MenuListActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.TopActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>