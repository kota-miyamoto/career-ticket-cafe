<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.leverages.careerticketcafe">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".controllers.NewsDetailActivity"></activity>
        <activity android:name=".controllers.NewsListActivity" />
        <activity android:name=".controllers.Seminar.SeminarListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.Seminar.SeminarDetailActivity"></activity>
        <activity android:name=".controllers.MenuDetailActivity" />
        <activity android:name=".controllers.MenuListActivity" />
        <activity android:name=".controllers.LoginActivity" />
        <activity android:name=".controllers.TopActivity">
        </activity>
    </application>

</manifest>